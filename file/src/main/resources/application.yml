spring:
  application:
    name: chat-file-cloud
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
springdoc:
  api-docs:
    enabled: true
    path: /chat-file-cloud/v3/api-docs
server:
  port: ${PORT:8282}